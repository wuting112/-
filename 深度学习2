1.回归是能够为一个或多个自变量与因变量之间的关系建模，通常用来解决预测价格需求等问题。
2.y=wx+b，w表示权重即每个特征对预测值的影响，x表示特征，b表示偏置。
3.损失函数能够量化实际值与预测值之间的差距，通常选择用平方误差函数P56，常数1/2是方便求导后系数变为1。
4.噪声是数据中无法通过模型来预测的随机波动或错误。
5.P62生成数据集中有1000个样本，每个样本中有两个特征，特征权重分别是2，-3.4，偏置为4.2，然后生成1000乘2的矩阵，并且每个值从均值为0，标准差为1的正态分布中抽取，用于生成
数据特征，而标准差为0.01用于生成噪声。
6.synthetic_data合成数据，torch matmul矩阵乘法。
7.import numpy as np导入数据库numpy并用np代替numpy，import torch；from torch.utils import data;from d2l import torch as d2l.都是导入库的几种写法。
8.net=nn.Sequntial(nn.Linear(2.1)),nn是神经网络的缩写，创建一个全连接层，输入2，输出1，即输入2个特征，输出1个结果。
9.全连接层：当前层中每一个的神经元都分别与上一层的所有神经元相连接。例如当前层有128个神经元，上一层有64个神经元，那他们之间总共就有8192个连接。
