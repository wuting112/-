mport os#设置基础路径，os模块是操作系统基础模块，是为了让python代码能与window是，Linux交互

os.makedirs(os.path.join('..', 'data'), exist_ok=True)#os.makedirs创建文件夹；..代表的是上级目录，即当前所在文件夹的上一层文件夹
data_file = os.path.join('..', 'data', 'house_tiny.csv')#定义数据文件夹的路径
with open(data_file, 'w') as f:#打开文件；as f将文件对象赋值给f
    f.write('''NumRooms,RoofType,Price
NA,NA,127500
2,NA,106000
4,Slate,178100
NA,NA,140000''')#用csv格式写入数据，NA表示缺失值，文件是3乘4的表格NumRooms,RoofType,Price分别表示房间数量，屋顶类型，价格
#os模块能够跨平台交互，自动判断当前系统
inputs = pd.get_dummies(inputs, dummy_na=True)#pd是pandas库的别名；将NAN视为一个类别；同时将pave设为1，nan设为0，然后会将pave，nan缺失值分别设置为0，1
print(inputs)#打印输出结果
